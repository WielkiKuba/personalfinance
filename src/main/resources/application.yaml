spring:
  datasource:
    url: jdbc:mysql://jakubdomain.ddns.net:3306/personal_finance?serverTimezone=UTC&characterEncoding=UTF-8
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: personalfinance

server:
  port: 8080